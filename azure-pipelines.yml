variables:
  buildConfiguration: 'Release'

steps:
- task: DotNetCoreCLI@2
  inputs:
    command: 'publish'
    publishWebProjects: true
- task: AzureWebApp@1
  inputs:
    azureSubscription: 'devopsazure'
    appType: 'webAppLinux'
    appName: 'vivektestapp'
    package: '$(System.DefaultWorkingDirectory)/**/*.zip'