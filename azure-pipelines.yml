# Starter pipeline
- task: DotNetCoreCLI@2
  inputs:
    azureSubscription: 'Azure subscription 1(304ec522-63ac-4515-8fc8-beb3fef8d0d7)'
    command: 'run'
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: 'default'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(Build.SourcesDirectory)'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/output.zip'
    replaceExistingArchive: true
  displayName: 'Archive files'

- task: PublishBuildArtifacts@1
  inputs:
    pathToPublish: '$(Build.ArtifactStagingDirectory)'
    artifactName: 'drop'
  displayName: 'Publish Artifact: drop'

- task: AzureWebApp@1
  inputs:
    azureSubscription: 'devopsazure'
    appName: 'vivekwebtest'
    package: '$(Build.ArtifactStagingDirectory)/output.zip'
  displayName: 'Deploy to Azure Web App'
